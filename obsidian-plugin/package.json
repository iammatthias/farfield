{
  "name": "farfield-publisher",
  "version": "0.2.0",
  "description": "Obsidian plugin — publish notes and media to a Farfield backend over HTTP.",
  "private": true,
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit --skipLibCheck && node esbuild.config.mjs production"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "obsidian": "latest",
    "typescript": "^5.7.0"
  }
}
