//! `farfield` — the CLI.
//!
//! Commands: init, push, import, post, snapshot, gc, schema, codegen, status.
//! Every mutating command is `--dry-run`-capable.
//!
//! Not yet implemented — this is the scaffolded entry point.

fn main() {
    eprintln!("farfield: not yet implemented (scaffold)");
}
