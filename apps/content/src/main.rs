//! `farfield-content` — the content service (`content.farfield.systems`).
//!
//! A thin binary over the shared records engine: serves the `post`,
//! `project`, `recipe`, and `art` collections from its own SQLite DB.
//!
//! Not yet implemented — this is the scaffolded entry point.

fn main() {
    eprintln!("farfield-content: not yet implemented (scaffold)");
}
