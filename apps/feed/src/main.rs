//! `farfield-feed` — the feed service (`feed.farfield.systems`).
//!
//! A thin binary over the shared records engine, identical in behaviour to
//! the content service: serves the `feed` collection from its own SQLite DB.
//! RSS and rendering are the website's job, not the backend's.
//!
//! Not yet implemented — this is the scaffolded entry point.

fn main() {
    eprintln!("farfield-feed: not yet implemented (scaffold)");
}
