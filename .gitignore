# Compiled binaries.
/bin/
farfield-content
farfield-feed
farfield-blobs
/farfield

# Local state — never committed.
*.db
*.db-wal
*.db-shm
/farfield.toml
.env
/farfield-blobs-data/
*.snapshot

# Secrets live in env / EnvironmentFile, never in the repo.
