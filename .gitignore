# Compiled binaries.
/bin/
farfield-content
farfield-feed
farfield-blobs
/farfield

# Local state — never committed.
*.db
*.db-wal
*.db-shm
/farfield.toml
.env
/farfield-blobs-data/
*.snapshot
PLAN.md
.DS_Store

# Secrets live in env / EnvironmentFile, never in the repo.
