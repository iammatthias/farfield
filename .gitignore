/target
**/*.rs.bk
*.pdb

# Local state — never committed.
*.db
*.db-wal
*.db-shm
/farfield.toml
.env
*.snapshot

# Secrets live in env / EnvironmentFile, never in the repo.

# blob service local-backend data
/farfield-blobs-data/
