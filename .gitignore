# Compiled binaries.
/bin/
/apps/apex/apex
/apps/backup/backup
/apps/blobs/blobs
/apps/calendar/calendar
/apps/content/content
/apps/feed/feed

# Local state — never committed.
*.sqlite
*.sqlite-shm
*.sqlite-wal
/data/

# Secrets live in env / EnvironmentFile, never in the repo.
.env

# Editor / OS cruft.
.DS_Store
PLAN.md
.gstack/

# Local operational skill — deploy runbook, not part of the shipped repo.
.claude/skills/farfield-deploy/

# Personal Claude Code permission overrides — not team-wide.
.claude/settings.local.json
