{
  "id": "series",
  "version": 1,
  "description": "An ordered group of images — a gallery. `refs` lists media CIDs in display order. No series records exist in obsidian_cms yet (galleries are currently posts with many inline images); this schema is here for when explicit galleries are authored.",
  "type": "object",
  "required": ["refs", "created"],
  "properties": {
    "title": {
      "type": "string",
      "description": "Gallery title. Optional."
    },
    "description": {
      "type": "string",
      "description": "Gallery description. Optional."
    },
    "refs": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Media blob CIDs, in display order."
    },
    "created": {
      "type": "datetime",
      "description": "When the gallery was created."
    }
  }
}
