---
description: post to the feed; trailing #hashtags become tags
---

Run exactly this, substituting the arguments given:

```sh
farfield feed $ARGUMENTS
```

Usage: `/feed <text...>`

Report only what the command printed — no preamble, no summary, no explanation of what the command does. If it fails, report the error verbatim.
